
<div class="article">
  <%= item.body %>
	<%= link_to h(item.title), item.source, :class => "source" unless item.source.blank? %>

  <%= link_to 'from %s' % item.source_short, item.source, :class => 'source' unless item.blank? %>
</div>