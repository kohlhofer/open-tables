<h1>Editing RSS Feed</h1>

<%= error_messages_for :feed %>

<% form_for(@feed, :html => { :method => :put}) do |f| %>
  
  <%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @feed %> |
<%= link_to 'Back', feeds_path %>
